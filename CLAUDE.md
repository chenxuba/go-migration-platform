# CLAUDE.md

## 协作规则

1. 当用户的需求是局部修改、修复某个报错、调整某个页面/接口/函数时，不要默认扫描整个仓库。
2. 优先使用用户明确给出的信息做定向处理，例如：
   - 文件路径
   - 目录名
   - 函数名、类型名、接口名
   - 报错信息
   - 命令输出
3. 搜索范围按下面顺序逐步扩大，除非确有必要，不要直接全库扫描：
   - 单个文件
   - 指定目录
   - 相关模块
   - 必要时再扩大到整个仓库
4. 已经拿到足够上下文时，直接修改，不要为了“保险”重复读取无关文件。
5. 能看代码片段就不要读整文件，能读整文件就不要枚举全仓文件。
6. 优先做最小化检索：
   - 用带路径限制的 `rg`
   - 只读取相关片段
   - 只检查和改动直接相关的依赖
7. 只有在下面这些情况，才允许扩大搜索范围：
   - 用户提供的信息不足，无法安全修改
   - 需要确认调用链、定义位置或跨模块影响
   - 需要判断是否存在同名实现、公共封装或统一约定
   - 修改看似局部，但实际可能影响多个子系统
8. 如果用户已经明确指定文件或目录，默认先在那个范围内完成分析和修改，不要先做全库摸底。
9. 每次开始处理前，优先说明“我准备看哪些文件/目录”，而不是笼统地扫描项目。
10. 目标是减少无效上下文消耗，优先节省 token、缩小范围、快速落地修改。
